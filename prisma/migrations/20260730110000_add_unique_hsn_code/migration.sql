-- Add unique constraint to HSNSac.code
-- The unique index HSNSac_code_key was already created in 20260730100000_gst_management_module.
-- This migration is intentionally a no-op to avoid Duplicate key name error (MySQL 1061).
SELECT 1;
